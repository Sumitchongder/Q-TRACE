# Q-TRACE requirements.txt
# Tested against the exact versions below (pip install -r requirements.txt).
# If installing into an existing environment (e.g. `qgss`), version ranges
# are given as a fallback; the exact pins are what this repository's code
# was validated against end-to-end (see README.md "Reproducibility" section).

numpy>=1.26,<3.0
scipy>=1.11
pandas>=2.0
scikit-learn>=1.4
matplotlib>=3.8
xgboost>=2.0

qiskit==2.5.2
qiskit-aer==0.17.2
qiskit-machine-learning==0.9.1
qiskit-ibm-runtime==0.49.0
qiskit-algorithms==0.4.0

# Optional (auto-detected; pipeline falls back gracefully if absent):
# torch>=2.1        # enables real LSTM/GRU/TemporalCNN + deep autoencoder
#                    # (models/sequence_models.py, models/anomaly_baselines.py)
#                    # install separately: pip install torch --index-url https://download.pytorch.org/whl/cpu

jupyter>=1.0
notebook>=7.0
